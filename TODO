1. 增加molparser基类，自动从hf/modelscope下载moldet & molparser-mobile，支持cpu/gpu调用，并增加对应能力的skills
2. 支持多种输入，比如单个图像，单个pdf，多个图像，多个pdf等。pdf时rec_only=False，图像时可选rec_only=True；输入可以是PIL Image，也可以是图像路径，也可以是图像url，pdf url，或者以上的list
渲染pdf识别，支持pdf分子抽取等功能实现